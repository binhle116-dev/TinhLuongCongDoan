# PROJECT PROGRESS

Append-only ticket history. **Never edit or delete a prior line.** Add
exactly one new line whenever `Current Ticket` in
`docs/01_GOVERNANCE/PROJECT_SNAPSHOT.md` changes (a ticket closes or a
new one activates). This file has no other purpose — it is not a design
doc, not a snapshot, and not a place for discussion.

Format: `YYYY-MM-DD | TICKET_ID | outcome (one line)`

---

2026-08-27 | MILESTONE-1-PHAT-MVP | COMPLETED / TECHNICAL SELF-VERIFIED. Django `core`+`phat` apps built: RBAC scoped by post office (Admin/Phòng ban/Trưởng bưu cục), daily raw-data import (`SanLuongChiTiet_*.xlsx`) with idempotent re-import, admin-editable service/price mapping tables (left empty pending TCHC/TCKH confirmation), provisional piece-rate calculation, allowance entry, Excel export, unmatched-mapping report. 10/10 automated tests pass; manual browser walkthrough across 3 roles confirmed correct scoping (404 on cross-office access, 403 on admin-only page). No PO UI PASS recorded yet under this governance model (predates this document set).
2026-08-27 | PRICING-DATA-SEED | COMPLETED / TECHNICAL SELF-VERIFIED (DEC-009). Added `phat/management/commands/seed_pricing_data.py`: populates ServiceCategory (48) / PriceGroup (10) / PriceCard (480) from the real "Đơn giá XD 2026" price table, RouteGroupMapping (221) from the real "Tuyến" sheet (verified 117/117 exact match against real route codes — no code transformation needed), and ServiceMapping (47 rules) derived from real observed data combinations, resolving 98.6% of real rows (6611/6706). 9 known-ambiguous combinations deliberately left unmapped. Fixed a real bug found along the way: `slugify()` was silently colliding "<=2kg"/">2kg" category names into one code, overwriting price data — fixed by including column index in the generated code. Payroll totals still not end-to-end validated (0 employees loaded yet).
2026-08-27 | EMPLOYEE-DATA-IMPORT | COMPLETED / TECHNICAL SELF-VERIFIED (DEC-010). Added `phat/management/commands/import_employees.py`: imported 159 real employees from "DS nhân sự TTVH (27.8.2026).xlsx" across 7 post offices + new `TTVH` management unit, per PO-clarified org mapping (Thuận Hóa/Hoá spelling merge, Khai thác vs. 7 named transport-worker reassignments — corrected once from an initial 8-name list to the right 7, TTVH center staff). Backfilled postman_code for 115/159 from March 2026 data by Mã HRM match. Recomputed provisional pay for the one real daily file on hand: 96 employees now resolve to a real nonzero total (20,386,369.20đ for 2026-08-26 alone) — first genuine end-to-end nonzero result. 56 postman codes in real raw data and 44 employees still unmatched — visible in `/bao-cao/chua-anh-xa/`, not yet resolved.
2026-08-27 | POSTMAN-CODE-STALENESS-FIX | COMPLETED / TECHNICAL SELF-VERIFIED (DEC-011). Found that March-backfilled postman_code values can go stale (route reassignment over time): cross-checked all 115 by name against April 2026 reference data, found 7 stale (didn't appear in real August data at all), corrected them (verified no collision with any other employee first). Switched `import_employees.py`'s backfill source to the April "mã BT" sheet (validated 100% name-correct) instead of March's "TH-TP-Chốt". Re-import after the fix: 96→103 employees matched, provisional total +1,758,206đ. Remaining 49 unresolved postman codes correspond mainly to ~15 newly-hired employees (HRM 0027xx/0028xx) absent from every historical file available — needs current route data from operations, not derivable from existing files.
2026-08-27 | NEW-HIRE-POSTMAN-CODES | COMPLETED / TECHNICAL SELF-VERIFIED (DEC-012). Product Owner supplied postman_code for 14 of the 15 newly-hired employees listed in the prior ticket; applied after confirming no collision with any existing employee. 1 (00279833 NGUYỄN VĂN HIẾU) left without a code — source list said "Mất tích" instead of giving a route code, not guessed. 1 supplied code (00279646 NGUYỄN XUÂN LONG) was flagged "Không hoạt động" in the source but applied as given without touching `Employee.is_active` (different, broader meaning — deferred to PO). Re-import: 103→112 employees matched, provisional total +633,990.10đ (22,778,565.15đ total for 2026-08-26). 40 postman codes in real data remain unmatched.
2026-08-27 | MARK-EMPLOYEE-INACTIVE | COMPLETED (DEC-013). PO confirmed 00279646 NGUYỄN XUÂN LONG should be inactive; `Employee.is_active` set to `False`.
2026-08-27 | AUGUST-MONTH-IMPORT | COMPLETED / TECHNICAL SELF-VERIFIED (DEC-014). Downloaded and imported all 26 available daily `SanLuongChiTiet` files for August 2026 (01-26; 27 not yet exported). Fixed the WinSCP saved-site name in scripts/README/PROJECT_CONTEXT (real name `cas_hue@10.1.45.10`, not the originally-documented `CAS_Hue_SFTP`). Caught and killed an over-broad unscoped `-neweronly` pull mid-run that would have downloaded the entire remote history (2+ years, 300+ files) instead of just August; added `scripts/pull_august_only.txt` for scoped backfills going forward. Found and correctly handled 2 broken source exports (`SanLuongChiTiet_01082026.xlsx`/`..._02082026.xlsx` are 1-row placeholders — used the real `..._001.xlsx` versions instead). Full month now in the system: 121 employees resolve to a nonzero provisional total, month sum 523,891,850.70đ.
2026-08-27 | UI-SIDEBAR-REDESIGN | COMPLETED / TECHNICAL SELF-VERIFIED, PO UI Check Required: Yes — awaiting PO review (DEC-015). Redesigned all templates around a left sidebar (module-grouped nav, disabled placeholders for the 3 future công đoạn) and modern shared component CSS (`base.html`), self-contained (no external CDN). Added a post-office ("bc") filter on the Lương page and Excel export, scoped through the existing RBAC mechanism. Added `phat.context_processors.current_period`. Verified in a real browser: sidebar renders on every page, BCVH filter correctly narrows both summary and detail tables (tested Thuận Hóa: 46 employees, 235,313,373đ), login page restyled, re-login works. 10/10 automated tests still pass.
2026-08-28 | KHAITHAC-MODULE-MVP | COMPLETED / TECHNICAL SELF-VERIFIED, PO UI Check Required: Yes — awaiting PO review (DEC-016). New Django app `khaithac` (KTC1 Huế 1, mã `530100`): direct SQL Server import (`import_khaithac_production`, decrypts DPAPI-protected password in memory via pywin32, never logged) from 2 databases — `BCCP530100_2024` for 11 raw Loại (R/E/C/U variants) and `BCCP530900` for KT1 (initially misattributed to the wrong database — PO corrected, verified 2,135 items/month vs. 2 from the wrong source). Payroll formula implemented per VB1054/TB-BĐHUE (11/3/2026) + VB1182/TB-BĐHUE (31/3/2026 rate adjustment): Quỹ tiền lương = Σ(sản lượng theo Nhóm dịch vụ × đơn giá), 4 groups only (EMS/GHI_SO/BUU_KIEN/PHBC), date-versioned pricing (`KhaiThacPriceCard`), admin-editable raw→nhóm mapping (`KhaiThacServiceMapping`) with `KT1` deliberately left unmapped (VB1054/1182 don't define a group for it; needs PO/TCHC confirmation — surfaced at `/khai-thac/chua-anh-xa/`). Per-employee split by Hệ số ca (VB1054 mục 1.3) NOT YET implemented — needs a real shift roster (`KhaiThacShiftAssignment`, `KhaiThacQualityCoefficient` models created but empty, no data to guess from). July 2026 verified end-to-end: 106,675 raw rows imported (exact match against previously-validated SQL query totals), fund = 32,244,500đ (KT1's 2,135 items correctly excluded, not guessed). 3/3 new automated tests pass; sidebar's "Khai thác" placeholder replaced with a working link; verified in a real browser session.
2026-08-28 | KHAITHAC-SHIFT-ROSTER | COMPLETED / TECHNICAL SELF-VERIFIED, PO UI Check Required: Yes — awaiting PO review (DEC-017). PO supplied the real timesheet ("BCC hệ số 2026 (LT).xlsx"), unblocking the per-employee split left open by DEC-016. Added `import_khaithac_shift_roster` (parses the real per-day/per-shift structure, stops before the sheet's own TỔNG row) and redesigned `KhaiThacShiftAssignment` to store the hệ số exactly as the org recorded it (not re-derived — some rows, e.g. "GSS" duty, use non-standard values like 0.4). Found and fixed a real bug during verification: name-matching via Django's `__iexact` silently failed for ALL 18 distinct roster names because SQLite's `UPPER()` doesn't case-fold Vietnamese diacritics — switched to Python-side `.upper()` comparison, after which 293/293 rows matched real employees (0 genuinely unmatched; a "Lãnh đạo TTVH" entry that looked unmatched at first was just the sheet's sign-off line, correctly excluded by stopping before TỔNG). New `khaithac/services/employee_pay.py` implements VB1054 §1.3's fund-splitting formula exactly. July 2026 verified end-to-end: imported total hệ số (302.6) matches the sheet's own TỔNG cell exactly; 17 employees paid between 159,837đ and 3,974,619đ, summing exactly to the 32,244,500đ fund. 3 new tests (12/12 total for khaithac+phat) pass; verified in a real browser session.
2026-08-28 | KHAITHAC-KT1-MAPPING-CONFIRMED | COMPLETED (DEC-018). PO confirmed the 2 open questions from DEC-016/017: `KT1` (M-prefix items) maps to Nhóm EMS, and the Hệ số chất lượng default of 1.0 for everyone is the actual intended rule (not a placeholder pending "Phụ lục 01"). Updated `seed_khaithac_pricing.py` and re-seeded; no Loại is now unmapped. July 2026 fund recomputed: 32,244,500đ → 32,921,295đ (+676,795đ = 2,135 KT1 items × 317đ EMS rate). 12/12 tests still pass.
2026-08-28 | PHAT-SERVICEMAPPING-KT1-RESOLVED | COMPLETED / TECHNICAL SELF-VERIFIED (DEC-019). PO showed the live LAN app's `/bao-cao/chua-anh-xa/` page (confirming the earlier LAN connectivity issue is fixed) with 19 unmapped Phát combos — more than the 9 originally logged in DEC-009, since August's data surfaced new variants. Resolved 13 by cross-checking the real price table: "KT1 A/B/C" and "KT1 Hỏa tốc - A/B/C" verified to share the exact same PriceCard as the already-mapped "KT1 ABC"/"KT1 Hỏa Tốc ABC" (VB1054 p.6 row 10 bundles all these product names under one price), plus a new rule for "C-Bưu kiện" QT (single international rate, no weight split). Added `rematch_unmatched` management command (re-applies current ServiceMapping/postman_code to already-imported RawDailyProduction without re-reading source files) and ran it against all of August 2026: 2,425 rows newly matched, provisional total rose from 523,891,850.70đ to 542,680,336.05đ (+18,788,485.35đ — a real correction). 6 combos deliberately left unmapped: "C-Báo Phát"/"M-Báo Phát" (unclear if this product exists via service C/M, only confirmed via R/E), "Gói nhỏ thường" QT, and "KT1 Hỏa tốc Hẹn giờ" (with/without "-C") — found the org's own price file has dedicated "KT1 hẹn giờ"/"hẹn giờ ABC" columns priced at exactly 0 across every Nhóm, genuinely unclear whether that's intentional (a free/bundled service) or a gap in their own spreadsheet; not guessed either way. 12/12 tests still pass.
2026-08-28 | KHAITHAC-LAN-RESTART-AND-AUGUST-IMPORT | COMPLETED. PO reported module Khai thác showing as disabled on the LAN app (10.47.33.10:8000) — root cause: the standing `serve.py` process (started 2026-08-27, before khaithac existed) doesn't hot-reload code, so it was still serving the pre-Khai-thác build. Restarted it (PO confirmed first, since it briefly interrupts anyone on the LAN) — module now live. Imported August 2026 Khai thác production (28 days, 98,542 items, fund 30,139,370đ, no unmapped Loại) per PO's request; August's shift roster intentionally left un-imported pending the PO's file, so `/khai-thac/2026/8/` shows the fund total but no per-employee split yet.
2026-08-28 | NHANSU-TONGQUAN-SPLIT | COMPLETED / TECHNICAL SELF-VERIFIED, PO UI Check Required: Yes — awaiting PO review (DEC-020). Split "Nhân sự" and "Tổng quan" out of module Công đoạn Phát into their own top-level sidebar sections (both are shared across all công đoạn). Moved Employee CRUD from `phat` to `core` (same URL names, nothing else needed updating); moved `base.html`/`current_period` context processor to `core` since it's now the shared UI shell. New `core.views.overview_dashboard` (now the login landing page) shows Lương per công đoạn side by side with Năm/Tháng + Bưu cục filters, aggregating Phát's `EmployeeMonthlyPay` and Khai thác's fund/employee-share services, RBAC-scoped. Found and fixed a real pre-existing bug: `payroll_detail`'s bưu cục dropdown used `scope_queryset()`'s default field name against the `PostOffice` model itself (which has no such field) — silently worked only for Admin/superuser, would have crashed with `FieldError` for any real Trưởng bưu cục visiting the Lương page, never caught because all prior UI testing used the admin account. Fixed with a new `scope_post_office_choices()` helper + regression test. 21/21 tests pass (up from 12).
2026-08-28 | EMPLOYEE-LIST-GROUPED-BY-OFFICE | COMPLETED / TECHNICAL SELF-VERIFIED (DEC-021). PO asked the Nhân viên page to group by bưu cục with a selector, showing only that office's employees (previously Admin/Phòng ban saw all offices mixed in one long list). `employee_list` now defaults to the first bưu cục in scope (forced to the user's own office for Trưởng bưu cục, unchanged), filters strictly by the selected `?bc=`, and dropped the now-redundant per-row "Bưu cục" column. Edit/cancel flows preserve the `bc` selection so the admin doesn't lose their place. 23/23 tests pass (2 new, covering the default-office and bc-switch behavior for Admin). Verified in a real browser session.
