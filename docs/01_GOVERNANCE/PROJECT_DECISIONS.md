# PROJECT DECISIONS

Numbered, frozen decision log. An entry here is authoritative once the
Product Owner has actually approved it — Claude Code may draft an entry
but must not mark it approved on its own (see `DOCUMENT_GOVERNANCE.md`
Section 1). Do not edit or delete a numbered entry once recorded;
supersede it with a new entry that references the old one if it changes.

| ID | Date | Decision | Status |
| --- | --- | --- | --- |
| DEC-001 | 2026-08-27 | This project uses a single-executor governance model: Claude Code plays both the coordinator ("CTO") and implementer role in one session, reporting directly to the Product Owner. There is no separate Claude-chat coordinator role. | Approved |
| DEC-002 | 2026-08-27 | The `Antigravity` (UI/UX + Windows runtime specialist) and `Codex` (legacy implementation engineer) roles from the prior project's governance model are dropped entirely — no such tools exist in this environment and this project has no historical Codex work to preserve. | Approved |
| DEC-003 | 2026-08-27 | Module Phát's rebuild (Milestone 1) uses Django + SQLite (WAL mode) + waitress, run via Windows Task Scheduler (or NSSM later if reliability requires it), chosen for a non-professional-developer Admin on Windows, internal LAN/VPN only, modest concurrency. See `PROJECT_CONTEXT.md` Section 7. | Approved |
| DEC-004 | 2026-08-27 | Module Phát's piece-rate pay uses **"đơn giá chốt nội bộ BĐTP Huế"** as its pricing basis, not the official "đơn giá TCT" — even though the two are known to disagree (see `PROJECT_CONTEXT.md` Section 6). This carries forward a decision already made in an earlier prototype, validated against real July 2026 figures. | Approved |
| DEC-005 | 2026-08-27 | `ServiceMapping`, `RouteGroupMapping`, and `PriceCard` are intentionally left empty at Milestone 1 delivery rather than populated with inferred/guessed values, because the service-code-to-category mapping and the route-to-price-group mapping are not fully confirmed (see `PROJECT_CONTEXT.md` Section 6). Every "công theo sản lượng" figure reads as 0 until these are populated via Django admin. This is by design, not a defect. | Approved |
| DEC-006 | 2026-08-27 | The governance document rename `CODEX_DOCUMENTATION_STANDARD.md` → `DEVELOPE_DOCUMENTATION_STANDARD.md` (proposed by Claude Code for naming consistency) is confirmed by the Product Owner. | Approved |
| DEC-007 | 2026-08-27 | Connecting this repository to `https://github.com/binhle116-dev/TinhLuongCongDoan.git` (git init, remote add, first push) is authorized by the Product Owner. | Approved |
| DEC-008 | 2026-08-27 | The prior project's `AUTO-BACKFILL-UI_PLAN.md` example is recreated as `docs/10_TICKETS/PRICING_MAPPING_BACKEND_FIRST_PLAN.md`, renamed because "AUTO-BACKFILL-UI" is meaningless outside the source project — content illustrates the same backend-first/UI-second principle using Module Phát's real pricing/mapping work instead. | Approved |
| DEC-009 | 2026-08-27 | Supersedes the "leave empty" part of `DEC-005`: the Product Owner directed populating `ServiceCategory`/`PriceGroup`/`PriceCard`/`RouteGroupMapping`/`ServiceMapping` from available real data (`phat/management/commands/seed_pricing_data.py`), rather than continuing to wait for TCHC/TCKH confirmation. Populated with evidence-based rules only (see `PROJECT_CONTEXT.md` Section 6 update) — 9 combinations already known to be ambiguous remain deliberately unmapped, still surfaced via `/bao-cao/chua-anh-xa/`. This is NOT the same as TCHC/TCKH confirmation — it is a good-faith, data-derived population the Product Owner explicitly asked for; treat computed totals as still provisional until validated against a known full-month total. | Approved |
| DEC-010 | 2026-08-27 | Real personnel data (159 employees, file "DS nhân sự TTVH (27.8.2026).xlsx") imported via `phat/management/commands/import_employees.py`, per explicit Product Owner clarification on 3 org-mapping points: (1) "BCVH Thuận Hóa"/"BCVH Thuận Hoá" (2 spellings in source file) are the same office, code `533140`; (2) "Bưu cục Khai thác" = KTC1 Huế 1 (`530100`), **except** 7 named employees (HRM `00266919, 00262607, 00267222, 00272438, 00252099, 00238601, 00264613`) who are reassigned to `533140` with position "200 - Công nhân vận chuyển bưu chính" (a corrected list — the Product Owner's first message listed 8 names including `00273849` TRƯƠNG VĂN DŨNG, then corrected to these 7, leaving Dũng at KTC1 Huế 1 with his original position); (3) 8 center-level management staff ("TRUNG TÂM VẬN HÀNH", oversee all post offices) are assigned to a new non-delivery `PostOffice` record with code `TTVH` created for this purpose. `postman_code` (needed to link to raw production data) was not in this personnel file — 115/159 employees were backfilled from March 2026 `TH-TP-Chốt` data by matching `Mã HRM`; the remaining 44, plus 56 distinct postman codes seen in real raw data with no employee match, are a known residual gap (stale March mapping and/or CTV staff not in this roster), not a defect. | Approved |
| DEC-011 | 2026-08-27 | `postman_code` values backfilled from March 2026 data (`DEC-010`) are not fully reliable — routes/postman assignments can change over time. Cross-checking all 115 backfilled entries by name against the more recent April 2026 "mã BT" reference sheet found 7 stale codes (didn't appear at all in real August production data) with a confirmed correct replacement (name-matched, no collision with any other employee); the other 108 validated as correct (name matches exactly). Corrected the 7; switched `import_employees.py`'s backfill source from March's "TH-TP-Chốt" sheet to April's "mã BT" sheet going forward, since it validated 100% correct. Re-importing the one real daily file after the fix raised matched employees from 96 to 103 and the provisional total by 1,758,206đ — a real, material correction, not cosmetic. 49 postman codes in real data (roughly matching 15 newly-hired employees with HRM codes in the 0027xx/0028xx range, not present in any historical file available) remain genuinely unresolved — need current route assignment from the operations team, not something derivable from existing files. | Approved |
| DEC-012 | 2026-08-27 | Product Owner supplied postman_code for 14 of the 15 newly-hired employees flagged in `DEC-011` (from an operations-provided list with route status). 1 remains unresolved: `00279833` NGUYỄN VĂN HIẾU, whose entry read "Mất tích" (no code given) instead of a route code — left without a postman_code rather than guessed; needs explicit follow-up with operations on this person's status. One supplied code (`00279646` NGUYỄN XUÂN LONG, `53B390`) was marked "Không hoạt động" (inactive) in the source list — the code was still applied as given, but `Employee.is_active` was NOT changed automatically since that field carries a different, broader meaning (employment status) than a single route's current activity note; ask the Product Owner explicitly before touching it. Re-import after applying the 14 codes: matched employees rose from 103 to 112, provisional total by 633,990.10đ. 40 postman codes in real data remain unmatched. | Approved |
| DEC-013 | 2026-08-27 | Product Owner confirmed: `00279646` NGUYỄN XUÂN LONG (flagged "Không hoạt động" in `DEC-012`) should be marked inactive. `Employee.is_active` set to `False`. | Approved |
| DEC-014 | 2026-08-27 | Downloaded and imported all of August 2026's daily `SanLuongChiTiet` files (26 days, 01-26/08) instead of just one day. Found 2 real issues along the way, both fixed: (1) the WinSCP saved site is actually named `cas_hue@10.1.45.10` (WinSCP's auto-generated default), not `CAS_Hue_SFTP` as originally documented — `scripts/pull_sanluong_chitiet.txt`, `README.md`, and `PROJECT_CONTEXT.md` corrected; (2) an unscoped `-neweronly` wildcard pull (`SanLuongChiTiet_*.xlsx`) matches the ENTIRE remote history (mid-2024 onward, 300+ files) since none existed locally yet — this was caught mid-run, killed, and replaced with a month-scoped pattern (`scripts/pull_august_only.txt`, `*082026*`) for this kind of targeted backfill; the standing daily script is unaffected since it only ever needs "today". Also found `SanLuongChiTiet_01082026.xlsx` and `..._02082026.xlsx` (no suffix) are broken 1-row placeholder exports for those two days — the real data is in the `..._001.xlsx` suffixed versions, which were used instead. Full month now imported: 121 employees resolve to a nonzero provisional total, sum 523,891,850.70đ across the 26 days on hand (2026-08-27 not yet exported by the source system). | Approved |

| DEC-015 | 2026-08-27 | Product Owner requested a visual redesign: left sidebar navigation grouped by module ("Công đoạn Phát" active items; "Công đoạn khác" showing Thu Gom/Vận chuyển/Khai thác as disabled placeholders, previewing the eventual 4-module structure), plus a post-office selector on the Lương (payroll) page. Implemented via `phat/templates/base.html` (sidebar + topbar + shared component CSS, self-contained, no external CDN — internal LAN app must work without internet), a new `phat.context_processors.current_period` (so the sidebar's "Lương" link always points at the current month), and a `bc` query-param filter on `payroll_detail`/`export_excel` scoped through the existing `core.permissions.scope_queryset` (a Trưởng bưu cục still only ever sees their own office in the dropdown). All other templates restyled to match; RBAC and existing tests unaffected (10/10 still pass). | Approved |

| DEC-016 | 2026-08-28 | Module Khai thac (buu cuc KTC1 Hue 1, ma `530100`) bat dau bang mot app Django moi `khaithac`, tach rieng khoi `phat`: (1) Nguon du lieu san luong la SQL Server truc tiep (khong phai file Excel qua SFTP nhu Phat) - da xac minh 2 database: `BCCP530100_2024` (PosCode=530100, loai tru TransferMachine='530100-HUE') cho 11 Loai R/E/C/U, va rieng `BCCP530900` (PosCode=530900, TransferMachine='HUE-KTVC-KT1') cho Loai 'KT1' (ma buu gui prefix 'M') - ban dau doan nham KT1 nam trong BCCP530100_2024 (chi ra ~2 dong/thang), PO xac nhan lai va kiem tra thuc te cho thay BCCP530900 moi la nguon dung (~2.135 dong/thang, hop ly hon nhieu). Mat khau SQL Server dung file DPAPI-encrypted do chinh nguoi dung tao (`~/.khaithac_sql_pw.txt`), giai ma trong bo nho bang `pywin32` (`win32crypt.CryptUnprotectData`) ngay truoc khi mo ket noi - khong bao gio in/log ra. (2) Cong thuc tinh Quy tien luong Khai thac can cu dung VB1054/TB-BDHUE (11/3/2026, muc II.1) va VB1182/TB-BDHUE (31/3/2026, dieu chinh don gia tu 03/2026): `Quy tien luong = Sum(San luong theo Nhom dich vu x Don gia)`, chi co 4 Nhom (EMS/GHI_SO/BUU_KIEN/PHBC), KHONG phan biet theo can nang hay noi tinh/quoc te (khac Phat). 12 Loai tho anh xa vao 4 Nhom qua `KhaiThacServiceMapping` (admin-editable); rieng Loai 'KT1' (M-prefix) CO CHU DINH de trong (chua anh xa) vi VB1054/1182 khong nhac den mot Nhom rieng cho no trong bang don gia Khai thac (chu "KT1" trong bang don gia Cong doan Phat la ten 1 san pham khac, trung ten ngau nhien) - can PO/TCHC xac dinh KT1(M) thuoc Nhom nao truoc khi tinh vao quy, hien surface qua `/khai-thac/chua-anh-xa/`. Don gia luu theo 2 ky hieu luc (01/2026 va 03/2026 tro di) qua `KhaiThacPriceCard`. (3) Viec chia Quy tien luong cho tung nhan vien theo He so ca lam viec (VB1054 muc 1.3: ca chuan=1.0, truong ca=1.2, khong du 1 ca thi quy doi theo gio thuc te) VAN CHUA lam duoc vi CHUA CO du lieu Bang phan ca thuc te (ai truc ca nao, ngay nao, co phai truong ca khong) - da tao san model `KhaiThacShiftAssignment` va `KhaiThacQualityCoefficient` (he so chat luong thang, Phu luc 01 VB1054 - mac dinh 1.0/Dat, cung chua co noi dung Phu luc 01 that) nhung de trong, khong doan. Da kiem chung: import thang 7/2026 (106.675 dong tho, khop 100% voi ket qua truy van SQL da xac minh truoc do), Quy tien luong tinh duoc = 32.244.500d (KT1 2.135 cai bi loai vi chua anh xa, dung thiet ke). 3/3 test tu dong pass. Kiem tra qua trinh duyet that (dang nhap, xem trang Quy tien luong + trang Chua anh xa) - PO UI Check Required: Yes, chua co PO PASS. | Approved |

| DEC-017 | 2026-08-28 | Product Owner cung cap file cham cong that "BCC he so <nam> (LT).xlsx" (1 sheet/thang, vd `T7.26` cho 07/2026) lam Bang phan ca thuc te con thieu tu DEC-016. Xay `import_khaithac_shift_roster` doc dung cau truc that (cot NGAY/CONG VIEC/TEN/HE SO HDC/CA, dong TEN trong = cho chua xep nguoi, bo qua; dung truoc dong TONG cuoi sheet). Doi lai model `KhaiThacShiftAssignment`: luu THANG DUNG gia tri he_so tu file (khong tu tinh lai qua is_truong_ca/actual_hours nhu thiet ke ban dau o DEC-016, vi thuc te co truong hop khac 1.0/1.2 chuan nhu cong viec "GSS" = 0.4); them `raw_name` + `employee` nullable de van luu duoc dong khong khop ten (khong roi mat khoi tong he so, chi khong co nguoi nhan). Khop ten voi Employee.full_name qua so sanh `.upper()` trong Python (KHONG dung QuerySet `__iexact` vi SQLite `UPPER()` mac dinh khong xu ly dung ky tu co dau tieng Viet - phat hien duoc trong luc kiem thu, ban dau tuong nham 8/18 ten "khong khop" nhung thuc ra la loi ham UPPER cua SQLite). Sau khi sua: 293/293 dong khop 100% voi nhan vien co san (khong co dong nao that su unmatched trong du lieu thang 7) - dong "Lanh dao TTVH" ban dau tuong la 1 dong phan ca khong khop duoc hoa ra chi la nhan chu ky duyet o cuoi sheet, khong phai du lieu ca lam viec, importer da tu dung dung truoc dong TONG nen khong bi anh huong. Tong he so nhap lai (302.6) khop chinh xac voi dong TONG tu tinh san trong file cua don vi - xac nhan parser dung. Them `khaithac/services/employee_pay.py` (`compute_employee_shares`) trien khai dung cong thuc VB1054 muc 1.3 (dien giai theo mau muc 3.3/3.4 Xu ly nghiep vu cua chinh van ban): Don gia binh quan = Quy tien luong / Tong he so ca toan don vi; Tien luong lao dong i = Don gia binh quan x Tong he so ca cua i x He so chat luong thang cua i (mac dinh 1.0/Dat, Phu luc 01 van chua co). Da kiem chung thang 7/2026: 17 nhan vien nhan luong tu 159.837d den 3.974.619d, tong cong bang dung Quy tien luong 32.244.500d (khong co he so chua gan trong thuc te). 3 test moi (tong cong 12/12 test module Khai thac+Phat) pass; kiem tra qua trinh duyet that. | Approved |

## How to add a decision

1. Draft the row with the next sequential `DEC-0xx` ID, today's date, and a one-sentence decision statement.
2. Mark its Status `Proposed` until the Product Owner has actually approved it in conversation.
3. Once approved, update Status to `Approved` in the same edit — do not leave it `Proposed` after approval is given.
4. If a later decision changes an earlier one, add a new row that says so explicitly (e.g. "Supersedes DEC-004") rather than editing the old row.
